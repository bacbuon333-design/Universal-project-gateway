# QUANT RESEARCH PROGRAM STATUS SNAPSHOT — BEFORE V3.7.1 EXECUTION

## CURRENT BOTTLENECK

The program is currently blocked at the DATA AUTHORIZATION layer, not at strategy implementation.

Legacy `AlphaLab_Antigravity/data/GOLD_M30.csv`:

- structural integrity: PASS;
- SHA-256: `979d7e410efbdc17e37ecae7f6a37ed30ba1844097bd012f2b90dd6d0be4f5f0`;
- timestamp semantic: UNRESOLVED;
- timezone: NAIVE_UNRESOLVED;
- source lineage: UNRESOLVED;
- research eligibility: BLOCKED.

Therefore no new strategy chapter is authorized on that legacy dataset.

## CLOSED RESEARCH CHAPTERS

### H-204 through H-208

Closed after strict precommit reconciliation. No validly reproduced historical candidate passed the Distributed Edge Standard. Ambiguous specifications remain classified as ambiguous rather than silently resolved.

### H-209 through H-214

New-mechanism batch substantially improved opportunity density, but no configuration passed the distributed-edge standard. Subsequent audit repaired evaluation-window consistency, profit-concentration denominator and cost-contract reporting.

### H-215 through H-217

Directional follow-up showed that Long-only variants improved several historical PF values, but none passed the frozen distributed-edge gates. Directional asymmetry was retained only as a clue.

### H-218 through H-220

Previous-day breakout and normalized impulse families failed economically. LMDC H-220 produced a concentrated historical clue but failed temporal robustness.

### V3.6 LMDC event study

The mechanism study did not provide a valid basis for H-221. Frozen Rule 4 and Rule 5 were triggered, and exact horizon interpretation later became semantically unresolved because legacy bar timestamp meaning could not be proven.

### V3.6.1 semantic closure

Previous `LMDC MECHANISM WEAK / REGIME-DEPENDENT` label was withdrawn.

Official status:

`V3.6 EXACT HORIZON INTERPRETATION AMBIGUOUS — TIMESTAMP SEMANTICS UNRESOLVED`

LMDC chapter is closed.

## DATA GOVERNANCE CHAPTER

### V3.7

Canonical data contract introduced fail-closed authorization.

Legacy dataset result:

`RESEARCH_ELIGIBILITY = BLOCKED`

Structural cleanliness was explicitly separated from provenance.

### V3.7.1 — CURRENT

Controlled canonical re-export is designed and frozen before execution.

New lineage target:

- `GOLD_M30_CANONICAL.csv`;
- direct MetaTrader5 Python `copy_rates_range` extraction;
- explicit broker/server/symbol identity;
- `BAR_OPEN_TIME` semantic tied to the exact source API;
- explicit UTC;
- exact hash-bound sidecar;
- deterministic transform chain;
- no overwrite of legacy data.

No strategy research is authorized during V3.7.1.

## HARD RESEARCH STANDARD TO CARRY FORWARD

When strategy research eventually resumes, retain the full distributed-edge standard:

- every complete quarter >= 5 trades;
- every full year >= 20 trades;
- max quarter trade share <= 5%;
- max quarterly trades / median <= 3;
- trade-count Gini < 0.30;
- Top-3 positive-quarter PnL share <= 40%;
- Top-5 <= 60%;
- rolling 4Q positive >= 70%;
- rolling 4Q PF >= 1.20 in >= 65% windows;
- rolling 8Q positive >= 75%;
- rolling 8Q PF >= 1.20 in >= 70% windows;
- >= 70% complete calendar years profitable;
- PF >= 1.25 after frozen costs;
- net expectancy > 0;
- causal next-bar execution and pessimistic ambiguous-bar handling;
- no post-result threshold rescue;
- no candidate promotion without separate precommitted stability/future-validation stages.

## WHAT HAS ACTUALLY BEEN LEARNED

1. Sparse squeeze-style systems do not satisfy the project's required temporal distribution standard.
2. Increasing trade frequency alone does not create economic edge; H-209/H-218/H-219 demonstrated this clearly.
3. Directional asymmetry exists descriptively in some historical families, but did not survive the full robustness standard as a candidate.
4. A PF above 1.25 can still be unacceptable when profits are temporally concentrated; H-220-C4 demonstrated why the distribution gates matter.
5. Event-level mechanism analysis can falsify an attractive backtest narrative before more strategy tuning is attempted.
6. Data provenance and bar-time semantics are first-class research variables. A clean OHLC file without lineage is not scientifically authorized.

## CURRENT DECISION TREE

### If V3.7.1 export becomes ELIGIBLE

Stop V3.7.1 and independently audit the new dataset/provenance. Only after that may a new research chapter be precommitted.

Historical results on the legacy dataset are not automatically transferred or validated by the new dataset.

### If V3.7.1 remains BLOCKED

Do not start strategy research. Resolve the exact acquisition blocker or change to a separately precommitted known source.

## CURRENT PROGRAM STATUS

- Strategy survivor: NONE.
- Validated alpha: NONE.
- Historical distributed survivor awaiting future validation: NONE.
- Active strategy hypothesis: NONE.
- Active task: controlled canonical market-data acquisition and authorization.
- Research permission on legacy GOLD_M30: BLOCKED.
